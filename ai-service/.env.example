# Copy to .env. This service has NO database connection string — see
# app/core/config.py for why that's enforced architecturally, not just
# by convention. See AI module spec, project doc §8.1/§8.3.

CORE_BACKEND_BASE_URL=http://backend:8000/api/v1

# LLM provider — fill in whichever is chosen (project doc §8.7, open item)
LLM_PROVIDER=
LLM_API_KEY=

ENVIRONMENT=development
