# Copy to .env. Frontend env vars are shipped to the browser — never put
# backend secrets here (DB credentials, JWT secret). See rules.md §4 rule 4.

VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_AI_SERVICE_BASE_URL=http://localhost:8100
