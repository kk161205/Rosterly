# Route modules go here, one file per resource (auth.py, employees.py,
# assets.py, requests.py, ...) matching the endpoint tables in
# ROSTERLY_PROJECT_DOCUMENTATION.md §5. Register each router in
# app/main.py under settings.API_V1_PREFIX — see project doc §7 rule 6,
# every path in the doc is relative to /api/v1.
